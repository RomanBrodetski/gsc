<section id='body'>
  <div class="page-title">
    <%= (copywriting('pic', { :scope => 'header', :page => @page, :phrase_type => "wysiwyg"   }) || "").html_safe%>
    <h1>
      <%= (copywriting('title', { :scope => 'header', :page => @page, :phrase_type => "string"   }) || "").html_safe%>
    </h1>
  </div>
  <%=raw @page.content_for(:body) %>
</section>
<!-- <section id='side_body'>
  <%=raw @page.content_for(:side_body) %>
</section> -->
